{
  "name": "app-securesoft-mobile-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
